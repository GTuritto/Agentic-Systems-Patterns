# Minimal Agent Runtime

This folder supports the from-scratch mini-framework labs in the book.

Run the demo:

```bash
npm run mini-runtime
```

Run the tests:

```bash
npm run mini-runtime:test
```

The runtime is intentionally small. It shows the primitives that larger frameworks package: state, decisions, tool registry, policy gate, context packets, trace events, and trajectory evals.
